import 'package:flutter/painting.dart';

class ColorPalette {
  /// The accent color
  static const Color cerise = Color(0xFFDD2277);

  /// The accent color
  static const Color denim = Color(0xFF1177CC);

  /// The color for the light theme background, dark theme default text
  static const Color white = Color(0xFFFFFFFF);

  /// The color for the light theme default text
  static const Color lynch = Color(0xFF667799);

  /// The color for the dark theme background
  static const Color black = Color(0xFF000000);
}
